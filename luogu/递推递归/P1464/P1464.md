# P1464 [PacNW 1999] Function

## 题目描述

对于一个递归函数 $w(a,b,c)$


- 如果 $a \le 0$ 或 $b \le 0$ 或 $c \le 0$ 就返回值 $1$。
- 如果 $a>20$ 或 $b>20$ 或 $c>20$ 就返回 $w(20,20,20)$
- 如果 $a<b$ 并且 $b<c$ 就返回 $w(a,b,c-1)+w(a,b-1,c-1)-w(a,b-1,c)$。
- 其它的情况就返回 $w(a-1,b,c)+w(a-1,b-1,c)+w(a-1,b,c-1)-w(a-1,b-1,c-1)$


这是个简单的递归函数，但实现起来可能会有些问题。当 $a,b,c$ 均为 $15$ 时，调用的次数将非常的多。你要想个办法才行。

注意：例如 $w(30,-1,0)$ 又满足条件 $1$ 又满足条件 $2$，请按照最上面的条件来算，答案为 $1$。

## 输入格式

会有若干行。

并以 $-1,-1,-1$ 结束。

## 输出格式

输出若干行，每一行格式：

`w(a, b, c) = ans`

注意空格。

## 输入输出样例 #1

### 输入 #1

```
1 1 1
2 2 2
-1 -1 -1
```

### 输出 #1

```
w(1, 1, 1) = 2
w(2, 2, 2) = 4
```

## 说明/提示

### 数据规模与约定

保证输入的数在 $[-9223372036854775808,9223372036854775807]$ 之间，并且是整数。

保证不包括 $-1, -1, -1$ 的输入行数 $T$ 满足 $1 \leq T \leq 10 ^ 5$。''





# 题解AC代码

	#include<iostream>
	#include<cstdio>
	using namespace std;
	typedef long long ll;  //手懒得打long long
	
	ll a,b,c,note[30][30][30] = {0}; //记忆初始化，记得都要用ll哦
	
	ll w(ll i,ll j,ll k){
		if(i<=0||j<=0||k<=0) return 1;  //第一种情况
		if(i>20||j>20||k>20) return w(20,20,20);//第二种
		if(i<j&&j<k){//第三种
			if(note[i][j][k-1] == 0){//是否有记忆
				note[i][j][k-1] = w(i,j,k-1);
			}
			if(note[i][j-1][k-1] == 0){
				note[i][j-1][k-1] = w(i,j-1,k-1);
			}
			if(note[i][j-1][k] == 0){
				note[i][j-1][k] = w(i,j-1,k);
			}
			note[i][j][k] = note[i][j][k-1] + note[i][j-1][k-1] - note[i][j-1][k]; //返回
		}
		else{//其他情况
			if(note[i-1][j][k] == 0){
				note[i-1][j][k] = w(i-1,j,k);
			}
			if(note[i-1][j-1][k] == 0){
				note[i-1][j-1][k] = w(i-1,j-1,k);
			}
			if(note[i-1][j][k-1] == 0){
				note[i-1][j][k-1] = w(i-1,j,k-1);
			}
			if(note[i-1][j-1][k-1] == 0){
				note[i-1][j-1][k-1] = w(i-1,j-1,k-1);
			}
			note[i][j][k] = note[i-1][j][k] + note[i-1][j-1][k] +  note[i-1][j][k-1] - note[i-1][j-1][k-1]; 
		}
		return note[i][j][k];//结束
	}
	int main(){
		while(1){
			cin>>a>>b>>c;
			if(a==-1&&b==-1&&c==-1) return 0;
			cout<<"w("<<a<<", "<<b<<", "<<c<<')'<<" = "<<w(a,b,c)<<endl;
		}
	}
	

啊,这个AC代码真的是赏心悦目啊,用三维数组实现记忆化,return明确,函数名简单,变量名简约,结构清晰

以下是思路:

每次传3个数,严格按照题目说明,有一个小于0,就返回1

如果有大于20的,就返回w(20,20,20)

这里可大有学问,这里就是竞赛思维和工程思维的碰撞之处,你且看这题里面所有的情况无非就落在20以内,总共也就21^3的可能性,因为任何<0或者>20的组合都会无脑返回,那么这个时候,如果你还抱着严谨思维,要把一切读到的遍历到的东西记录下来,那么就会MLE;真正的严谨是在最小的空间最短的时间实现一样的性能;所以我的代码这在个地方高下立判,我还傻乎乎地去记下来这些个状态!

然后到了第三种if,先检查有没有记忆,没有记忆你就先去给我找一找这个记忆,找到了之后再返回给我,我就在这里但是我不会等着你;这就是类似异步的思维;

第四种情况同理

我觉得DP这种东西最神乎的地方就在这个return了,如果你连要return什么东西都不清不楚马马虎虎的,那你肯定写不出来这个DP,我要好好品味一下为什么要这么return



虽然这题在luogu上只是个普及-的题目,哈,对于熟练DP的神犇来说,肯定是水题切切

但是我们这种DP不熟练的蒟蒻,这题就是我们感受DP思想的绝佳范本

# 我的代码

​	

	#include <bits/stdc++.h>
	#include <tuple>
	using namespace std;
	#define int long long
	map<tuple<int,int,int>,int>mp;
	int dfs(int n1,int n2,int n3){
		
		if(mp.count({n1,n2,n3})){
			return mp[{n1,n2,n3}];
		}
		if(n1<=0||n2<=0||n3<=0){
			mp[{n1,n2,n3}]=1;
			return 1;
		}
		else if(n1>20||n2>20||n3>20){
			mp[{n1,n2,n3}]=dfs(20,20,20);
			return 	mp[{n1,n2,n3}];
		}
		else if(n1<n2&&n2<n3){
			if(!mp.count({n1,n2,n3-1})){
				mp[{n1,n2,n3-1}]=dfs(n1,n2,n3-1);
			}
			if(!mp.count({n1,n2-1,n3-1})){
			mp[{n1,n2-1,n3-1}]=dfs(n1,n2-1,n3-1);
			}
			if(!mp.count({n1,n2-1,n3})){
				mp[{n1,n2-1,n3}]=dfs(n1,n2-1,n3);
			}
			mp[{n1,n2,n3}]=mp[{n1,n2,n3-1}]+mp[{n1,n2-1,n3-1}]-mp[{n1,n2-1,n3}];
			return 	mp[{n1,n2,n3}];
		}
		else{
		
			
			if(!mp.count({n1-1,n2,n3})){
				mp[{n1-1,n2,n3}]=dfs(n1-1,n2,n3);
			}
			if(!mp.count({n1-1,n2-1,n3})){
				mp[{n1-1,n2-1,n3}]=dfs(n1-1,n2-1,n3);
			}
			
			if(!mp.count({n1-1,n2,n3-1})){
				mp[{n1-1,n2,n3-1}]=dfs(n1-1,n2,n3-1);
			}
			
			if(!mp.count({n1-1,n2-1,n3-1})){
				mp[{n1-1,n2-1,n3-1}]=dfs(n1-1,n2-1,n3-1);
			}
			
			mp[{n1,n2,n3}]=mp[{n1-1,n2,n3}]+mp[{n1-1,n2-1,n3}]+mp[{n1-1,n2,n3-1}]-mp[{n1-1,n2-1,n3-1}];
			return mp[{n1,n2,n3}];
		}
		
	
	}
	signed main()
	{
		
		vector<vector<int>>num;
		while(1){
			int temp1,temp2,temp3;
			cin>>temp1>>temp2>>temp3;
			if(temp1!=-1&&temp2!=-1&&temp3!=-1){
				vector<int>temp;
				temp.push_back(temp1);
				temp.push_back(temp2);
				temp.push_back(temp3);
				num.push_back(temp);
			}
			else{
				break;
			}
		}
		int lim=num.size()-1;
		for(int i=0;i<=lim;i++){
			int n1=num[i][0];
			int n2=num[i][1];
			int n3=num[i][2];
			int ans=dfs(n1,n2,n3);
			cout<<"w("<<num[i][0]<<", "<<num[i][1]<<", "<<num[i][2]<<") = "<<ans<<endl;
		}
		return 0;
	}



# 我的反思



第一宗罪:装逼之罪,学了个unorderedmap,map啥的,就老是看到记忆化搜索就想用,不分青红皂白的用,急头白脸地用,惟恐天下不知道你会用unorderedmap;

你为什么可以这样瞎78乱用啊?!OI竞赛里根本不是这样!!!

你应该先看数据范围,小范围->数组

​			离散,稀疏地数据 ->哈希表

​			大数据->剪枝

不是一上来就哈希表的,咕咕嘎嘎!!!!



### 这有什么问题？

- `map` 是红黑树 👉 **O(log n)**
- `tuple` 作为 key 👉 **比较非常慢**
- 状态数量其实最多就：

```
21 × 21 × 21 = 9261
```

但你每次都在：

- 构造 tuple
- 查 map
- 插入 map

👉 实际复杂度远超你想象



第二宗罪!繁琐之罪!

main函数里面你为什么搞个二维vector,明明可以直接传参进去,你偏要先储存,然后再遍历,再传参

这里很想引用北京WB.梓墨选手的一句名言